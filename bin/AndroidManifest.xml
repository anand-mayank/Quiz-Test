<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="proj.proj"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        
        android:icon="@drawable/aa"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="proj.proj.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="proj.proj.Login"></activity>
<activity android:name="proj.proj.Signup"></activity>
<activity android:name="proj.proj.Test"></activity> 
<activity android:name="proj.proj.Maths"></activity>
<activity android:name="proj.proj.Python"></activity>
<activity android:name="proj.proj.Java"></activity>

        </application>

</manifest>
